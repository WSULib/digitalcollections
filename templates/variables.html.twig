{# Title #}
{% macro title(data) %}
	{{data.response.solr_doc.dc_title[0]}}
{% endmacro %}

{# Date #}
{% macro date(data) %}
	{% for date in data.response.solr_doc.dc_date %}
	{{date}}<br/>
	{% endfor %}
{% endmacro %}

{# Secondary Images -- if any #}
{% macro secondaryImages(data) %}
	{% for img in data.response.content_type_specific[0].imageParts.sorted %}
		<img src="http://192.168.42.5/loris/fedora:{{data.response.pid}}%7C{{img.preview}}/full/63,/0/default.jpg">
	{% endfor %}
{% endmacro %}

{# Downloads #}

{# Social Media #}

{# Permissions Link #}

{# MLA Citation #}

{# APA Citation #}